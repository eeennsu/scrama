import type { NextPage } from 'next'
import { PropsWithChildren } from 'react'

const DetailProductLayout: NextPage<PropsWithChildren> = ({ children }) => {
    return <>{children}</>
}

export default DetailProductLayout
