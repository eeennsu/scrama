{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node"
        }
    ],
    "build": {
        "env": {
            "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD": "true",
            "CHROME_BINARY_PATH": "/usr/bin/google-chrome-stable"
        }
    }
}
